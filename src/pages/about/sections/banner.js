import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledAboutBanner = styled(Box)(({ theme }) => ({

}))

const AboutBanner = () => {
	return (
		<StyledAboutBanner>
			<div>AboutBanner</div>
		</StyledAboutBanner>
	)
}

export default AboutBanner