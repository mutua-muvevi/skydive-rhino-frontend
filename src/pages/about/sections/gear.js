import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledGear = styled(Box)(({ theme }) => ({

}))

const Gear = () => {
	return (
		<StyledGear>
			<div>Gear</div>
		</StyledGear>
	)
}

export default Gear