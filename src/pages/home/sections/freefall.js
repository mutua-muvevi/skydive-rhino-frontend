import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledHomeFreefall = styled(Box)(({ theme }) => ({
}))

const HomeFreefall = () => {
	return (
		<StyledHomeFreefall>
			<div>HomeFreefall</div>
		</StyledHomeFreefall>
	)
}

export default HomeFreefall