import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledHomeHelicopter = styled(Box)(({ theme }) => ({
}))

const HomeHelicopter = () => {
	return (
		<StyledHomeHelicopter>
			<div>HomeHelicopter</div>
		</StyledHomeHelicopter>
	)
}

export default HomeHelicopter