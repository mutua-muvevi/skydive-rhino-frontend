import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledSchool = styled(Box)(({ theme }) => ({

}))

const School = () => {
	return (
		<StyledSchool>
			<div>School</div>
		</StyledSchool>
	)
}

export default School