import { Box } from "@mui/material";
import { styled } from "@mui/system";

const StyledMap = styled(Box)(({ theme }) => ({

}))

const Map = () => {
	return (
		<StyledMap>
			<div>Map</div>
		</StyledMap>
	)
}

export default Map